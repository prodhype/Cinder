import lexer


def main() -> i32:
    path: String = "bootstrap_lexer_input.ci"
    with open(path, "rb") as file:
        scanner = lexer.new_lexer(file.read_text())
        scanner.tokenize()
        return scanner.dump_with_path(&path)
