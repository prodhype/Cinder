def take() -> List[i32]:
    o: Option[List[i32]] = Some([1, 2, 3])
    match o:
        case Some(v):
            return v
        case None:
            return []

def main() -> i32:
    xs: List[i32] = take()
    print(len(xs), xs[0])
    return 0
