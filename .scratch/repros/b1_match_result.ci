enum E:
    a

def take() -> List[i32]:
    r: Result[List[i32], E] = Ok([10, 20])
    match r:
        case Ok(v):
            return v
        case Err(_):
            return []

def main() -> i32:
    xs = take()
    print(xs[0])
    return 0
