class Base:
    def work(self, value: i32) -> i32:
        return value + 1


class Child(Base):
    def run(self, value: i32) -> i32:
        return value + 100


def run(value: i32) -> i32:
    return value + 10


def action(value: i32) -> i32:
    return value
