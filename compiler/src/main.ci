import lexer


def main() -> i32:
    with open("bootstrap_lexer_input.ci", "rb") as file:
        scanner = lexer.new_lexer(file.read_text())
        scanner.tokenize()
        return scanner.dump()
