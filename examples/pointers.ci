def increment(value: &i32) -> void:
    value += 1


def main() -> i32:
    value: i32 = 40
    increment(value)
    pointer: *i32 = &value
    pointer[0] += 1
    print(f"value: {value}")
    return 0
