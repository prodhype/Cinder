from shapes import Shape
import math


@reflect
class Circle(Shape):
    radius: f64

    def __init__(self, radius: f64):
        self.radius = radius

    @override
    def area(self) -> f64:
        return math.pi * self.radius * self.radius
