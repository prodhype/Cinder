extern import "ctype.h"

extern "C":
    # a checked signature for a C library function
    def toupper(character: c_int) -> c_int


def main() -> i32:
    result = toupper('a')
    print(cast[char](result))
    return 0
